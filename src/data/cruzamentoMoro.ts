/**
 * Base de dados do Cruzamento Quali-Quanti (Moro).
 * SUBSTITUA este placeholder pelo conteúdo real quando o arquivo
 * `src/data/cruzamentoMoro.ts` original for anexado ao projeto.
 */
export const cruzamentoMoroData = {
  updatedAt: '2026-07-08',
  segments: [] as Array<Record<string, unknown>>,
  insights: [] as Array<{ id: string; title: string; description: string }>,
};

export type CruzamentoMoroData = typeof cruzamentoMoroData;
